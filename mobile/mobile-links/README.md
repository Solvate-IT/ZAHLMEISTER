# HTTPS App Links

Publish the following files without redirects:

- `https://<app-host>/.well-known/assetlinks.json` for Android
- `https://<app-host>/.well-known/apple-app-site-association` for iOS

Replace the signing placeholders only after the Android release certificate and Apple Team ID are final. Browser fallback remains available for all links.
