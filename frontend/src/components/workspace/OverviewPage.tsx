"use client";
import {useEffect,useState} from "react";
import {api} from "@/lib/api";
import type {CollectionSummary,ParticipantListSummary} from "@/lib/types";
import {useI18n} from "@/lib/i18n";
import {Loading,ErrorState,Empty} from "../State";
export function OverviewPage({onNavigate}:{onNavigate:(v:"lists"|"collections"|"settings")=>void}){const {t,locale}=useI18n();const [lists,setLists]=useState<ParticipantListSummary[]>([]);const [collections,setCollections]=useState<CollectionSummary[]>([]);const [state,setState]=useState<"loading"|"ok"|"error">("loading");async function load(){setState("loading");try{const [l,c]=await Promise.all([api.lists(),api.collections()]);setLists(l);setCollections(c);setState("ok")}catch{setState("error")}}useEffect(()=>{load()},[]);if(state==="loading")return <Loading/>;if(state==="error")return <ErrorState onRetry={load}/>;const participants=lists.reduce((s,l)=>s+l.participant_count,0);const total=collections.reduce((s,c)=>s+Number(c.amount)*c.participant_count,0);const paid=collections.reduce((s,c)=>s+Number(c.paid_amount),0);const money=(n:number)=>new Intl.NumberFormat(locale,{style:"currency",currency:collections[0]?.currency||"EUR"}).format(n);return <><div className="page-title"><div><h1>{t("overview")}</h1><div className="muted">{t("subtitle")}</div></div><div className="actions"><button className="button secondary" onClick={()=>onNavigate("lists")}>{t("newList")}</button><button className="button" onClick={()=>onNavigate("collections")}>{t("newCollection")}</button></div></div><div className="grid-4"><Metric label={t("participantLists")} value={lists.length}/><Metric label={t("participants")} value={participants}/><Metric label={t("total")} value={money(total)}/><Metric label={t("paidAmount")} value={money(paid)}/></div><div className="split"><section className="card"><div className="row between"><h3>{t("recentCollections")}</h3><button className="button ghost small" onClick={()=>onNavigate("collections")}>{t("all")}</button></div>{collections.length===0?<Empty text={t("noCollections")}/>:collections.slice(0,5).map(c=><div className="mini-row" key={c.id}><div><strong>{c.name}</strong><div className="muted">{t("paidOf",{paid:c.paid_count,total:c.participant_count})}</div><div className="progress"><span style={{width:`${c.participant_count?c.paid_count/c.participant_count*100:0}%`}}/></div></div><strong>{money(Number(c.paid_amount))}</strong></div>)}</section><section className="card"><div className="row between"><h3>{t("participantLists")}</h3><button className="button ghost small" onClick={()=>onNavigate("lists")}>{t("all")}</button></div>{lists.length===0?<Empty text={t("noLists")}/>:lists.slice(0,7).map(l=><div className="mini-row" key={l.id}><strong>{l.name}</strong><span>{t("people",{count:l.participant_count})}</span></div>)}</section></div></>}
function Metric({label,value}:{label:string;value:string|number}){return <div className="metric"><div className="muted">{label}</div><div className="value">{value}</div></div>}
